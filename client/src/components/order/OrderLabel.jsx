import React from "react";

const OrderLabel = () => {
  return (
    <label htmlFor="orderSelect" className="form-label fs-5 fw-bold text-dark">
      Ordenar por:
    </label>
  );
};

export default OrderLabel;
